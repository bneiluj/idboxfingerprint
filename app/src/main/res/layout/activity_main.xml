<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="io.idbox.fpsample.MainActivity">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay"/>

    </android.support.design.widget.AppBarLayout>

    <Button
        android:id="@+id/buttonEnrol"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:layout_marginTop="48dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/appBarLayout"
        android:theme="@style/ButtonColored"
        android:text="@string/enrol"
        android:onClick="clickEnrol"/>

    <Button
        android:id="@+id/buttonShowUser"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp"
        android:layout_below="@id/buttonEnrol"
        android:theme="@style/ButtonColored"
        android:text="@string/show_user"
        android:onClick="clickShowUser"/>

    <Button
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:layout_below="@+id/buttonShowUser"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp"
        android:theme="@style/ButtonColored"
        android:text="@string/pay_as_you_go"
        android:onClick="clickPayAsYouGo"/>

</RelativeLayout>
